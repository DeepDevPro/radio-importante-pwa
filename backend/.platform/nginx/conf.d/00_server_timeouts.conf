# Timeouts de servidor (contexto server - aplicado globalmente)
# Estes timeouts NÃO podem ficar em location {}

client_header_timeout 60s;
client_body_timeout   600s;  # 10 minutos para uploads grandes
send_timeout          300s;
