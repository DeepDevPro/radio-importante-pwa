name: DISABLED - Update Catalog

on:
  workflow_dispatch:

permissions: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Catalog updates should be run via admin tooling."